export const COLORS = {
  secondary: "#fff",
  default: "#fff",
  accent: "#fff",
  accentHover: "#fff",
  accentActive: "#fff",
  danger: "#fff",
  dangerActive: "#fff",
};
export const COLORS_RGBA = {
  secondary: (opacity: number = 1) => `rgba(0, 0, 0, ${opacity})`,
  default: (opacity: number = 1) => `rgba(0, 0, 0, ${opacity})`,
  accent: (opacity: number = 1) => `rgba(0, 0, 0, ${opacity})`,
  danger: (opacity: number = 1) => `rgba(0, 0, 0, ${opacity})`,
};
